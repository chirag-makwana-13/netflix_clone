import React from 'react';

const Header: React.FC = () => (
    <header style={{ padding: '10px', backgroundColor: '#111', color: '#fff' }}>
        <h1>Netflix Clone</h1>
    </header>
);

export default Header;
